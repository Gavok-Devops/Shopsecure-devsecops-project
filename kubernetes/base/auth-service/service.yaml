apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: shopsecure-prod
  labels:
    app: auth-service
    monitoring: enabled
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
