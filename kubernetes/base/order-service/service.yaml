apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: shopsecure-prod
  labels:
    app: order-service
    monitoring: enabled
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
